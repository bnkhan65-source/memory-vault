/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['10.0.0.3', 'physiology-terms-letters-tracker.trycloudflare.com', 'memory-vault-qrbb.vercel.app'],
};

export default nextConfig;
